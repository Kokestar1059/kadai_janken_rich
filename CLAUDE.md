# じゃんけんアプリ 要件定義

## 現在地（2026-05-22時点）

**ステータス: 仕様変更中（Phase 1〜8 完了済み・Phase 9〜15 未着手）**

旧仕様（あいこ固定）の実装は完了済み。  
これから新仕様（カウントダウン／AIが必ず勝つ／BGM／嫌味コメント）へ移行する。

→ 詳細な実装手順・コードヒントは **[HINTS.md](./HINTS.md)** を参照。  
→ 作業チェックリストは **[TODO.md](./TODO.md)** を参照。

### 完了済みの作業（旧仕様）
- HTML骨格・Tailwind CSSによるスタイリング
- カメラ映像のリアルタイム表示（`getUserMedia`）
- MediaPipe Gesture Recognizer によるグー/チョキ/パー認識
- AIが常にユーザーと同じ手を出す（あいこ固定）ロジック ← **廃止**
- JSを `script.js` に分離（`index.html` はUI専任）

### 既知の注意点（変更なし）
- MediaPipe の内部INFOログ（`Created TensorFlow Lite XNNPACK delegate for CPU`）はコンソールに出るが動作上の問題なし
- Tailwind CDN警告も同様に無視してよい（ローカル専用アプリのため）
- カメラは必ず `localhost` 経由で開くこと（`file://` 不可）

---

## 概要

PCのカメラを使ったリアルタイム画像認識じゃんけんアプリ。  
ユーザーが「じゃんけんをする」ボタンを押すと 3・2・1 のカウントダウンが始まり、  
1 の後にカメラのジェスチャーを認識して勝負する。AIは**必ずユーザーに勝つ手**を出す。

## 技術スタック

- HTML5
- JavaScript（バニラ）
- [MediaPipe Gesture Recognizer](https://ai.google.dev/edge/mediapipe/solutions/vision/gesture_recognizer) (CDN経由で読み込み)
- Web Audio API または `<audio>` タグ（BGM用）
- デプロイなし、ローカル実行のみ

## 機能要件

### カメラ入力
- PCのWebカメラをリアルタイムで取得（getUserMedia API）
- カメラ映像を常時画面に表示する（ゲーム中も表示し続ける）

### ゲーム開始ボタン
- 「じゃんけんをする」ボタンをクリックするとラウンドを開始
- カウントダウン中・結果表示中はボタンを無効化して連打を防ぐ

### カウントダウン
- ボタン押下後、画面中央に「3」→「2」→「1」と1秒ごとに表示
- 「1」の後（ぽん！のタイミング）にジェスチャーを1回だけキャプチャする
- カウントダウン中もカメラ映像は継続表示する

### ジェスチャー認識（MediaPipe Gesture Recognizer）
- **認識は1回のみ**（カウントダウン終了時点のスナップショット）
- リアルタイムループは「常時プレビュー用」に使い、結果取得は別途行う
- 以下のジェスチャーをじゃんけんの手に対応付ける:
  | MediaPipe ジェスチャー | じゃんけんの手 |
  |---|---|
  | `Closed_Fist` | グー ✊ |
  | `Open_Palm` | パー ✋ |
  | `Victory` | チョキ ✌️ |
- 上記以外のジェスチャーは「認識失敗」として扱い、再挑戦を促す

### AI の手（必ず勝つ）
- ユーザーの手を見て、**必ず勝てる手**を出す
  | ユーザー | AI（必ず勝つ手） |
  |---|---|
  | グー ✊ | パー ✋ |
  | パー ✋ | チョキ ✌️ |
  | チョキ ✌️ | グー ✊ |
- 認識失敗時はAIの手も「？」のまま、結果表示なし

### 嫌味コメント
- AIが勝ったとき、AIからの嫌味な定型文を1件ランダム表示する
- 定型文は10種類用意する（HINTS.md に一覧あり）
- 表示場所はUI下部の「コメントエリア」

### BGM
- アプリ起動後（またはボタン初回押下後）にBGMをループ再生する
- BGMファイルは `audio/bgm.mp3` に配置する（ファイルは別途調達が必要）
- ミュートボタンを設ける（任意）

### UI表示
- カメラ映像（リアルタイム）
- 「じゃんけんをする」ボタン
- カウントダウン表示エリア（3・2・1・ぽん！）
- ユーザーの手の認識結果（テキスト＋絵文字）
- AIの手（テキスト＋絵文字）
- 勝敗結果（AIが勝ったとき「AIの勝ち！」を表示）
- 嫌味コメントエリア
- 言語：日本語

## ファイル構成

```
rps/
├── index.html       # UI・スタイル（Tailwind CSS）
├── script.js        # カメラ初期化・MediaPipe・じゃんけん判定ロジック
├── audio/
│   └── bgm.mp3      # BGMファイル（別途調達）
├── TODO.md          # 実装チェックリスト
├── CLAUDE.md        # 本ファイル（仕様書）
└── HINTS.md         # 実装ヒント・手順書（新規）
```

## ローカルでの起動手順

1. プロジェクトディレクトリで以下を実行:
   ```
   python3 -m http.server 8080
   ```
2. ブラウザで `http://localhost:8080` を開く
3. 終了するときは `Ctrl + C`

> `getUserMedia`（カメラAPI）は `file://` で直接開くと動作しない。必ず `localhost` 経由で開くこと。

## 非機能要件

- 外部サーバーへのデプロイ不要（`python3 -m http.server 8080` でローカル動作）
- スタイルはシンプルで見やすいデザイン
- MediaPipe モデルの読み込み中はローディング表示
- HTTPS または localhost 環境でカメラアクセスが必要（ブラウザ制約）
